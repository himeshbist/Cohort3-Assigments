# Assigment-1

This is the Assigment-1 project from the Cohort-Assigment workspace.

Contents:

- `index.html` - main HTML file
- `style.css` - styles
- `assets/` - images and logos

This repository was prepared for initial commit and push to GitHub.

How to push to GitHub:

1. Create a repository on GitHub (name it e.g. `Assigment-1`).
2. Add the remote and push:

```
git remote add origin <REMOTE_URL>
git push -u origin main
```

If you'd like, I can create the remote repo for you if you tell me how you want to authenticate (GitHub CLI or a personal access token).
